apiVersion: v2
name: ai-pr-reviewer
description: AI-powered GitHub PR reviewer (API + worker + optional Postgres)
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: postgresql
    version: "15.5.38"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
