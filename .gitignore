# Java / Maven
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# Spring Boot
spring-boot-api/target/

# Azure Function worker
azure-function/target/
azure-function/bin/
azure-function/obj/
azure-function/dependency-reduced-pom.xml
# Dead since the Functions runtime was dropped, but may still exist locally
# with real secrets from before the conversion — keep it untracked.
azure-function/local.settings.json

# Helm (Chart.lock IS committed, charts/ is regenerated by `helm dependency build`)
helm/ai-pr-reviewer/charts/
helm/ai-pr-reviewer/values-secret.yaml

# k8s
k8s/secret.yaml

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/

# OS
.DS_Store

# Env / secrets
.env
*.env
PORTFOLIO_DETAILS.md
